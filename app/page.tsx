"use client";
import Link from "next/link";
import { CTA, Footer, Nav, services } from "../components/site";
import { useCmsRecords } from "../components/cms-public";
const homeFallback=[{id:"home-1",title:"More signal. Less chasing.",detail:"MATIG brings sharp research, intentional outreach, and great digital craft together to create B2B opportunities worth having.",status:"Published",updated:""}];
export default function Home(){const homepage=useCmsRecords("Homepage",homeFallback);const managedServices=useCmsRecords("Services",[]);const hero=homepage[0]||homeFallback[0];const serviceCards=managedServices.length?managedServices:services.map(([title,detail,slug])=>({id:slug,title,detail,status:"Published",updated:""}));return <><Nav/><main><section className="hero grid-bg"><div className="orb one"/><div className="orb two"/><div className="circuit"><i className="dot"/></div><div className="shell hero-copy"><span className="eyebrow">B2B growth, made human</span><h1>{hero.title}</h1><p>{hero.detail}</p><div className="hero-actions"><Link href="/contact" className="btn primary">Get a Free Sample →</Link><Link href="/services" className="btn secondary">Explore our services</Link></div><div className="proof"><div><strong>2.4×</strong><span>Average reply lift</span></div><div><strong>48hr</strong><span>Sample turnaround</span></div><div><strong>100%</strong><span>Human-checked data</span></div></div></div></section><section className="shell"><div className="section-head"><div><span className="eyebrow">What we do</span><h2>Every engagement starts with the right signal.</h2></div><p>We pair dedicated specialists with a practical, connected process—so every touchpoint feels considered.</p></div><div className="cards">{serviceCards.slice(0,3).map((item,index)=><Link className="card" href="/services" key={item.id}><span className="num">0{index+1} / {item.title}</span><h3>{item.title}</h3><p>{item.detail}</p></Link>)}</div></section><section style={{borderTop:"1px solid var(--line)",borderBottom:"1px solid var(--line)"}}><div className="shell split"><div><span className="eyebrow">The MATIG approach</span><p className="statement">We don’t believe a fuller pipeline starts with more noise. It starts with better context.</p><Link href="/about" className="btn secondary">Meet MATIG →</Link></div><div className="steps">{[["01","Map the market","We identify the accounts, people, and moments most worth pursuing."],["02","Create the signal","Our specialists build messages and assets that deserve attention."],["03","Make momentum","Clear reporting turns every campaign into smarter next steps."]].map(x=><div className="step" key={x[0]}><b>{x[0]}</b><div><h3>{x[1]}</h3><p>{x[2]}</p></div></div>)}</div></div></section><section className="band"><div className="shell"><span className="eyebrow">Built for ambitious teams</span><h2>Good growth work connects the dots between who you are and who you need to reach.</h2><div className="logos"><span>FINTECH</span><span>SAAS</span><span>HEALTHCARE</span><span>PROFESSIONAL SERVICES</span><span>MANUFACTURING</span></div></div></section><CTA/></main><Footer/></>}