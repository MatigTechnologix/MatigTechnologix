"use client";
import { CTA, Footer, PageHero } from "../../../components/site";
import { useCmsRecords } from "../../../components/cms-public";
const fallback=[{id:"about-1",title:"Growth is a people problem.",detail:"MATIG is a technology-enabled service agency for B2B teams that want more thoughtful ways to get in front of the right people.",status:"Published",updated:""}];
export default function About(){const records=useCmsRecords("About",fallback);const hero=records[0]||fallback[0];return <><PageHero eyebrow="About MATIG" title={hero.title} copy={hero.detail}/><main><section className="shell split"><div><span className="eyebrow">Our point of view</span><p className="statement">The best work happens when strategy, craft, and follow-through move together.</p></div><div className="steps">{[["01","Useful over loud","We make work that respects the attention of the person receiving it."],["02","Partners, not platforms","You get a responsive team that thinks alongside your own."],["03","Quality is a system","Research, review and refinement are part of every engagement."]].map(x=><div className="step" key={x[0]}><b>{x[0]}</b><div><h3>{x[1]}</h3><p>{x[2]}</p></div></div>)}</div></section><CTA/></main><Footer/></>}